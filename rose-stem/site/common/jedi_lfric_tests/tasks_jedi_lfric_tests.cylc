{# ########################################################################### #}
{# (c) Crown copyright 2024 Met Office. All rights reserved. #}
{# The file LICENCE, distributed with this code, contains details of the terms #}
{# under which the code may be used. #}
{# ########################################################################### #}
{% do LOG.debug("Entered site/common/da_dev/tasks_jedi_lfric_tests.cylc") %}

{% if task_ns.conf_name == "runge-kutta-C12" %}

    {% do task_dict.update({
        "opt_confs": ["runge-kutta"],
        "resolution": "C12",
        "DT": 1,
        "tsteps": 20,
        "kgo_checks": ["checksum"],
    }) %}

{% elif task_ns.conf_name == "integration_tests" %}

    {% do task_dict.update({
        "placeholder": true,
    }) %}

{% elif task.startswith("build") %}

    {% do task_dict.update({
        "placeholder": true,
    }) %}

{% endif %}


{# Set default values for Gravity Wave tasks #}
{% if task_dict %}
    {% do task_dict.update({"application_dir": "apps/applications/jedi_lfric_tests"}) %}
    {% if "app_name" not in task_dict %}
        {% do task_dict.update({"app_name": "jedi_lfric_tests"}) %}
    {% endif %}
{% endif %}

{% do LOG.debug("Finished in site/common/da_dev/tasks_jedi_lfric_tests.cylc") %}